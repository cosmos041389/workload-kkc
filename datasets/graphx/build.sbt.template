name := "test"

version := "1.0"

scalaVersion := "2.12.17"

artifactPath in (Compile, packageBin) := file("/home/cosmos/workload-kkc/datasets/graphx") / s"${name.value}.jar"

libraryDependencies ++= Seq(
    "org.apache.spark" %% "spark-sql" % "3.3.1",
    "org.apache.spark" %% "spark-graphx" % "3.3.1",
)
